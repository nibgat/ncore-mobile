import {
    StyleSheet
} from "react-native";

const stylesheet = StyleSheet.create({
    contentContainer: {
        justifyContent: "center",
        alignItems: "center",
        width: "100%"
    },
    seperator: {
        borderRadius: 2,
        width: "100%",
        height: 1
    }
});
export default stylesheet;
