import {
    StyleSheet
} from "react-native";

export default StyleSheet.create({
    buttonAreaContainer: {
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        height: 200
    }
});
