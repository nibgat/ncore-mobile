import {
    StyleSheet
} from "react-native";

const stylesheet = StyleSheet.create({
    bottomContainer: {
        justifyContent: "center",
        alignItems: "flex-end",
        width: "100%"
    },
    seperator: {
        borderRadius: 2,
        width: "100%",
        height: 1
    }
});
export default stylesheet;
