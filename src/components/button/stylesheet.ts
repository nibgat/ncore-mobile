import {
    StyleSheet
} from "react-native";

export default StyleSheet.create({
    container: {
        justifyContent: "center",
        flexDirection: "row",
        alignItems: "center"
    },
    loadingContainer: {
        position: "relative"
    },
    loading: {
        alignSelf: "center"
    }
});
