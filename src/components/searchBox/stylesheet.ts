import {
    StyleSheet
} from "react-native";

const styles = StyleSheet.create({
    container: {
        borderBottomWidth: 1,
        flexDirection: "row",
        alignItems: "center"
    },
    input: {
        borderColor: "transparent",
        flex: 1
    }
});
export default styles;
